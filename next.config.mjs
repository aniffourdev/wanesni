/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["wanesni.com"], // Allow images from your backend
  }
//   experimental: {
//     esmExternals: false,
//   },
};

export default nextConfig;
